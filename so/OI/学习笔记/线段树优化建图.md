---
title: 线段树优化建图
tags:
  - OI
categories:
  - [OI, 图论, 线段树优化建图]
mathjax: true
date: 2020-07-18 17:28:13
---

区间内边建图的优化。

<!--more-->

作为一个小白，我就先放上高义雄学长的讲解和例题。

# 线段树优化建图

学习笔记-$SGColin$-$2018/07/17$

## 适用情况

当点数很多，并且边集以区间形式（点$\rightarrow$区间$/$区间$\rightarrow$点）给出

强行连边复杂度太大，考虑如何维护同一个子区间的连边，使用线段树优化。

## 优化思路

先考虑点$\rightarrow$区间连边的部分。

考虑建图的复杂度真正体现在哪里。

如果所有的被连边区间不交，那么总复杂度是对的。

所以真正建图的复杂度，产生在被连边区间相交的部分，尤其是一个同一个区间被多个点连边，那么我们每一个都要产生区间长度的代价。

那么有一个思路，如果有一个区间被多次连边，那么不妨我们建立这个区间的一个代表节点。

首先预处理的时候将这个代表节点连向区间里的每一个节点，边权为$0$，就代表了连向代表节点的每一条边都连向了区间里的每一个节点，然后都每次都将边连向这个代表节点即可。

## 建图过程 (点$\rightarrow$区间)

但是注意到这种区间并不是很好找，这种找区间的方法并不是具有很强的普适性。

我们知道线段树的每一个节点维护的是一个固定的区间，线段树的区间操作是$log$的。

回忆区间加的过程，如果一个节点的代表区间被完全覆盖，那么我们会在这个节点上打标记，等到需要的时候再下传。这体现了一种代表的关系，即区间共有的性质我可以只标记在代表这个区间的节点上。

如果要从一个点连向一个区间，那么可以像区间加那样，把这个点向完全覆盖的区间连边。

也就是说，在区间连边时，在线段树上每找到一个完全覆盖的节点，就将出发点向这个区间的代表节点连边。

---

同样的，我们先考虑预处理。

一个节点被连边，代表这个节点的 **子树** 都可以到达。

之所以强调子树，而不是强调区间里的每一个节点，因为我们预处理的边只会向下 **连一层** 。

只连下一层的边数量级是$O(2N)$，而连向区间里的每一个节点显然每一层都是$O(N)$，总的边的数量级就是$O(Nlog_2N)$，边的级别显然大了很多，并且也不符合线段树的关系定义。

一个$8$个节点的线段树预处理之后的连边情况如图所示。图中边权全部为$0$ 。

{% asset_image 1.png %}

然后一条连边就可以按照我们说的那样了，找到对应的区间就好。

假如$8$号点要向区间$[2,5]$连边，最后会是蓝边的样子（边权就省略了）：

{% asset_image 2.png %}

## 建图过程 (区间$\rightarrow$点)

发现原来线段树的区间传递关系并不对了。

如果我们找到了每一段区间，直接向指向的点连边，子树内真正建边的点并没有连出这一条边。

如图，如果要区间$[5,8]$向$T$连边，蓝框框起来的点并没有真正向$T$连边。

因为建树的时候绿色边的方向是向区间内传递的。

{% asset_image 3.png %}

很容易想到反向再建一棵树。显然叶节点是相同的，所以两棵树会共用叶节点。

{% asset_image 4.png %}

倒着的树上传递关系就变为，一个节点连了出边，那么他所代表的区间里所有节点都向外连了一条出边。

同理，这些绿色的边的边权都是$0$。

关于正确性，显然更大的区间并不会到达小区间，所以连边不用担心范围超界。

此时我们就可以在倒着的树上找所有的区间，然后连出边就可以了。

## 例题

一张$N$个点的 **无向图** ，边以以下$M$条信息的形式给出：

- $l_1,r_1,l_2,r_2$代表区间$[l_1,r_1]$内的每一个点与区间$[l_2,r_2]$内的每一个点之间都有一条长度为$1$的无向边。

求$S$点的单源最短路，保证图连通。

- $N\le5\times10^5,M\le10^5,1\le l_1,l_2,r_1,r_2\le N$

我们对每一条信息都单独开出来一个转移节点，然后就变成了 区间$\rightarrow$点和 点$\rightarrow$区间的问题了。

但是双向边不能共用一个转移节点。因为这样其实上在$[l_1,r_1]$内的每个点之间都连了一条长度为$2$的边。

实际做的时候，对每一个单向边都开一个转移节点，然后出边边权都设为$0$，入边边权都设为$1$就可以了。

还有一个比较妙的方法，是出入边权都设为$1$然后最短路的答案除以$2$，也是可以的。

```cpp
#include <bits/stdc++.h>
#define LOCAL LOCAL
using namespace std;

namespace STDquantum {

#define intt register int
#define mid ((l + r) >> 1)
const int N = 4e6 + 10;
int n, m, s, cnt, tot, dis[N << 1], head[N << 1];
bool vis[N];
priority_queue<pair<int, int> > q;
struct E {
  int next, to, w;
} e[N << 1];
template <typename T> inline void read(T &x) {
  char ch;
  while (ch = getchar(), !isdigit(ch)) continue;
  x = (ch ^ 48);
  while (ch = getchar(), isdigit(ch)) x = x * 10 + (ch ^ 48);
}
inline void add(int from, int to, int w) {
  e[++tot] = (E){head[from], to, w};
  head[from] = tot;
}

struct In {
  int root, ptr;
  struct T {
    int ls, rs, p;
  } c[N << 1];

  inline int newnode() { return ++ptr; }
  void build(int &k, int l, int r) {
    k = newnode();
    if (l == r) {
      c[k].p = l;
      return;
    }
    build(c[k].ls, l, mid);
    build(c[k].rs, mid + 1, r);
    c[k].p = ++cnt;
    add(c[k].p, c[c[k].ls].p, 0);
    add(c[k].p, c[c[k].rs].p, 0);
  }
  void update(int k, int l, int r, int L, int R, int p, int w) {
    if (l > R || r < L) return;
    if (L <= l && r <= R) {
      add(p, c[k].p, w);
      return;
    }
    if (L <= mid) update(c[k].ls, l, mid, L, R, p, w);
    if (R > mid) update(c[k].rs, mid + 1, r, L, R, p, w);
  }
} treein;

struct Out {
  int root, ptr;
  struct T {
    int ls, rs, p;
  } c[N << 1];

  inline int newnode() { return ++ptr; }
  void build(int &k, int l, int r) {
    k = newnode();
    if (l == r) {
      c[k].p = l;
      return;
    }
    build(c[k].ls, l, mid);
    build(c[k].rs, mid + 1, r);
    c[k].p = ++cnt;
    add(c[c[k].ls].p, c[k].p, 0);
    add(c[c[k].rs].p, c[k].p, 0);
  }
  void update(int k, int l, int r, int L, int R, int p, int w) {
    if (l > R || r < L) return;
    if (L <= l && r <= R) {
      add(c[k].p, p, w);
      return;
    }
    if (L <= mid) update(c[k].ls, l, mid, L, R, p, w);
    if (R > mid) update(c[k].rs, mid + 1, r, L, R, p, w);
  }
} treeout;

inline void Dijkstra() {
  memset(dis, 0x3f, sizeof(dis));
  dis[s] = 0;
  q.push(make_pair(0, s));
  while (!q.empty()) {
    intt u = q.top().second;
    q.pop();
    if (vis[u]) continue;
    vis[u] = 1;
    for (intt i = head[u], v; i; i = e[i].next) {
      if (dis[v = e[i].to] > dis[u] + e[i].w) {
        dis[v] = dis[u] + e[i].w;
        q.push(make_pair(-dis[v], v));
      }
    }
  }
}
inline void main() {
  read(n), read(m), read(s);
  cnt = n;
  treein.build(treein.root, 1, n);
  treeout.build(treeout.root, 1, n);
  for (int i = 1, l1, r1, l2, r2; i <= m; ++i) {
    read(l1), read(r1), read(l2), read(r2);
    ++cnt;
    treein.update(treein.root, 1, n, l2, r2, cnt, 1);
    treeout.update(treeout.root, 1, n, l1, r1, cnt, 1);
    ++cnt;
    treein.update(treein.root, 1, n, l1, r1, cnt, 1);
    treeout.update(treeout.root, 1, n, l2, r2, cnt, 1);
  }
  Dijkstra();
  for (intt i = 1; i <= n; ++i) { printf("%d\n", dis[i] >> 1); }
}

}  // namespace STDquantum

int main() {
  STDquantum::main();
  return 0;
}
```

