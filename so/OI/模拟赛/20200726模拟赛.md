---
title: 20200726模拟赛
tags:
  - 模拟赛
  - OI
categories:
  - [OI, 模拟赛]
mathjax: true
date: 2020-07-26 09:47:52
---

> 但到头来，一切都是故事。
>
> <div align="right">——尤瓦尔·赫拉利《未来简史》</div>

<!--more-->

# 礼物

(gift.cpp\c\pas)

## 【问题描述】

夏川的生日就要到了。作为夏川形式上的男朋友，季堂打算给夏川买一些生日礼物。

商店里一共有种礼物。夏川每得到一种礼物，就会获得相应喜悦值Wi（**每种礼物的喜悦值不能重复获得**）。

每次，店员会按照一定的概率Pi（或者不拿出礼物），将第i种礼物拿出来。季堂每次都会将店员拿出来的礼物买下来。**没有拿出来视为什么都没有买到，也算一次购买。**

众所周知，白毛切开都是黑的。所以季堂希望最后夏川的喜悦值尽可能地高。

求夏川最后最大的喜悦值是多少，并求出使夏川得到这个喜悦值，季堂的期望购买次数。

## 【输入格式】

第一行，一个整数N，表示有N种礼物。

接下来N行，每行一个实数Pi和正整数Wi，表示第i种礼物被拿出来的概率和可以获得喜悦值。

## 【输出格式】

第一行，一个整数表示可以获得的最大喜悦值。

第二行，一个实数表示获得这个喜悦值的期望购买次数，保留3位小数。

## 【输入输出样例】

```
3
0.1 2
0.2 5
0.3 7
```

```
14
12.167
```

## 【数据范围】

对于10%的数据，N = 1

对于30%的数据，N ≤ 5

对于100%的数据，N ≤ 20 ，0 < Wi ≤ 10^9 ，0 < Pi ≤ 1且ΣPi ≤ 1

**注意：本题不设spj**

## 题解

看这数据范围，应该是个状压DP？

主要是样例模不出来，要么这题就切了。

这题意不甚明了，我尝试复述一下：每次店员都会拿出一个礼物，或者不拿，给出拿出每个礼物的概率，求拿出所有礼物的期望次数。

$w_i$不太重要，因为似乎每个礼物都只能选一次，那么的话直接加个和输出就行，剩下的就是概率期望的工作了。

设$s$为当前状态，每一位$1$对应已经选了，$0$对应没选。$f[s]$为买到当前状态的礼物的期望步数。

我们这次的期望步数肯定是要加一的，那么怎么从别的地方转移过来呢？

每次的可能就是要么不拿（或者拿到了已经拿过的东西，其实就相当于不拿），$f[s]*(1-\sum p)$，要么拿，$f[s\mbox{ xor }(1<<i)]\times p[i]$。到$(1<<n)-1$都拿了就不用再枚举了。

然后枚举的时候开一个变量记录一下所有包含在$i$中的元素的$\sum p[i]$，然后我们观察我们计算的这个式子
$$
f[s]=\sum_{i\in s} f[s\mbox{ xor }(1<<i)]\times p[i]+f[s]*(1-\sum_{i\in s}p[i])+1
$$
我们发现直接把右边的$f[s]$移到左边，变成了
$$
\sum_{i\in s}p[i]\times f[s]=\sum_{i\in s} f[s\mbox{ xor }(1<<i)]\times p[i]+1\\f[s]=\frac{\sum_{i\in s} f[s\mbox{ xor }(1<<i)]\times p[i]+1}{\sum_{i\in s}p[i]}
$$


这样就可以直接进行DP辣。

AC代码

```cpp
#include <bits/stdc++.h>
#define LOCAL
using namespace std;

namespace STDquantum {

typedef long long ll;
const int N = 102, M = 21;
ll n, yi = 1, maxx, a[N];
double sum, tmp1, p[N], f[1 << M];
inline void main() {
  scanf("%lld", &n);
  for (ll i = 0; i < n; ++i) {
    scanf("%lf%lld", &p[i], &a[i]);
    maxx += a[i];
  }
  printf("%lld\n", maxx);
  for (ll i = 1; i < (1 << n); ++i) {
    tmp1 = 0;
    for (ll j = 0; j < n; ++j) {
      if (i & (yi << j)) {
        tmp1 += p[j], f[i] += f[i ^ (yi << j)] * p[j];
      }
    }
    f[i] += 1;
    f[i] /= tmp1;
  }
  printf("%.3lf", f[(1 << n) - 1]);
}

}  // namespace STDquantum

int main() {
#ifndef ONLINE_JUDGE
#ifdef LOCAL
  freopen("test.in", "r", stdin);
  freopen("test.out", "w", stdout);
#endif
#ifndef LOCAL
  freopen("gift.in", "r", stdin);
  freopen("gift.out", "w", stdout);
#endif
#endif
  STDquantum::main();
  return 0;
}
```

# 通讯

(message.cpp\c\pas)

## 【问题描述】

“这一切都是命运石之门的选择。”

试图研制时间机器的机关SERN截获了中二科学家伦太郎发往过去的一条短信，并由此得知了伦太郎制作出了电话微波炉（仮）。

为了掌握时间机器的技术，SERN总部必须尽快将这个消息通过地下秘密通讯网络，传达到所有分部。

SERN共有N个部门（总部编号为0），通讯网络有M条单向通讯线路，每条线路有一个固定的通讯花费Ci。

为了保密，消息的传递只能按照固定的方式进行：从一个已知消息的部门向另一个与它有线路的部门传递（可能存在多条通信线路）。我们定义总费用为所有部门传递消息的费用和。

幸运的是，如果两个部门可以直接或间接地相互传递消息（即能按照上述方法将信息由X传递到Y，同时能由Y传递到X），我们就可以忽略它们之间的花费。

由于资金问题（预算都花在粒子对撞机上了），SERN总部的工程师希望知道，达到目标的最小花费是多少。

##【输入格式】

多组数据，文件以2个0结尾。

每组数据第一行，一个整数N，表示有N个包括总部的部门（从0开始编号）。

然后是一个整数M，表示有M条单向通讯线路。

接下来M行，每行三个整数,Xi,Yi,Ci，表示第i条线路从Xi连向Yi，花费为Ci。

## 【输出格式】

每组数据一行，一个整数表示达到目标的最小花费。

## 【输入输出样例】

```
3 3
0 1 100
1 2 50
0 2 100
3 3
0 1 100
1 2 50
2 1 100
2 2
0 1 50
0 1 100
0 0
```

```
150
100
50
```

## 【样例解释】

第一组数据：总部把消息传给分部1，分部1再传给分部2.总费用：100+50=150.

第二组数据：总部把消息传给分部1，由于分部1和分部2可以互相传递消息，所以分部1可以无费用把消息传给2.总费用：100+0=100.

第三组数据：总部把消息传给分部1，最小费用为50.总费用：50.

## 【数据范围】

对于10%的数据，保证M=N-1

对于另30%的数据，N ≤ 20 ，M ≤ 20

对于100%的数据，N ≤ 50000 ，M ≤ 10^5 ，Ci ≤ 10^5 ，数据组数≤ 5

数据保证一定可以将信息传递到所有部门。

## 题解

观察题目，题里说能够互相到达的点就不算了，这不就是连通块吗？

所以Tarjan缩点。

然后怎么做呢？Kruskal？

这是个有向图，Kruskal会爆的（只有十分/kk）。

所以考虑拓扑排序。然后每次跑的时候，从上一层到这一层的边里边权最小的边转移过来就好辣qwq

代码：

```cpp
#include <bits/stdc++.h>
#define LOCAL
using namespace std;

namespace STDquantum {

typedef long long ll;
const int N = 1e5 + 10, M = 2e5 + 10;
ll n, m, tot, head[N];
ll gg, top, sta[N], id[N], in[N], dfn[N], low[N];
ll in1[N], tot1, head1[N], dis[N];
struct E {
  ll next, from, to, w;
  inline friend bool operator<(const E &a, const E &b) {
    return a.w < b.w;
  }
} e[M], b[M];
void init() {
  memset(head, 0, sizeof(head));
  memset(head1, 0, sizeof(head1));
  memset(e, 0, sizeof(e));
  memset(b, 0, sizeof(b));
  memset(id, 0, sizeof(id));
  memset(in, 0, sizeof(in));
  memset(dfn, 0, sizeof(dfn));
  memset(low, 0, sizeof(low));
  memset(in1, 0, sizeof(in1));
  memset(dis, 0x3f, sizeof(dis));
  tot = tot1 = top = gg = 0;
}
inline void add(ll x, ll y, ll z) {
  e[++tot] = (E){head[x], x, y, z};
  head[x] = tot;
  // printf("add succeed:%lld->%lld (%lld)\n", x, y, z);
}
void Tarjan(ll s) {
  dfn[s] = low[s] = ++tot;
  sta[++top] = s, in[s] = 1;
  for (ll i = head[s], v; i; i = e[i].next) {
    if (!dfn[v = e[i].to]) {
      Tarjan(v);
      low[s] = min(low[s], low[v]);
    } else if (in[v]) {
      low[s] = min(low[s], dfn[v]);
    }
  }
  ll k;
  if (low[s] == dfn[s]) {
    ++gg;
    do {
      k = sta[top--];
      in[k] = 0, id[k] = gg;
    } while (s != k);
  }
}
inline void add1(ll x, ll y, ll z) {
  b[++tot1] = (E){head1[x], x, y, z};
  head1[x] = tot1;
  // printf("add1 succeed:%lld->%lld (%lld)\n", x, y, z);
}
inline void topo(int s) {
  queue<int> q;
  q.push(s), dis[s] = 0;
  ll wei = 0;
  while (!q.empty()) {
    ll t = q.front();
    q.pop();
    for (ll i = head1[t]; i; i = b[i].next) {
      dis[b[i].to] = min(dis[b[i].to], b[i].w);
      if (!(--in1[b[i].to])) q.push(b[i].to);
    }
    wei += dis[t];
  }
  printf("%lld\n", wei);
}
inline void main() {
  while (~scanf("%lld%lld", &n, &m)) {
    if (!n && !m) return;
    init();
    for (ll i = 1, x, y, z; i <= m; ++i) {
      scanf("%lld%lld%lld", &x, &y, &z);
      add(x, y, z);
    }
    tot = 0;
    for (ll i = 0; i < n; ++i) {
      if (!dfn[i]) Tarjan(i);
    }
    for (ll i = 0; i < n; ++i) {
      for (ll j = head[i]; j; j = e[j].next) {
        if (id[i] != id[e[j].to]) {
          add1(id[i], id[e[j].to], e[j].w);
          ++in1[id[e[j].to]];
        }
      }
    }
    topo(id[0]);
  }
}

}  // namespace STDquantum

int main() {
#ifndef ONLINE_JUDGE
#ifdef LOCAL
  freopen("test.in", "r", stdin);
  freopen("test.out", "w", stdout);
#endif
#ifndef LOCAL
  freopen("message.in", "r", stdin);
  freopen("message.out", "w", stdout);
#endif
#endif
  STDquantum::main();
  return 0;
}
```

# 奇袭

## UPD on 20200907

突然发现这题和[CF526F Pudding Monsters](https://www.luogu.com.cn/problem/CF526F)是一个题。正好之前标程咕咕咕了。补充一下。然后又发现[CF997E Good Subsegments](https://www.luogu.com.cn/problem/CF997E)是前面那个题的加强版。

嗯。。。。直接写后面那个题的解释吧。

见<a href="../cf997e-good-subsegments">这篇博客</a>。

##【问题描述】

由于各种原因，桐人现在被困在Under World（以下简称UW）中，而UW马上要迎来最终的压力测试——魔界入侵。

唯一一个神一般存在的Administrator被消灭了，靠原本的整合骑士的力量是远远不够的。所以爱丽丝动员了UW全体人民，与整合骑士一起抗击魔族。

在UW的驻地可以隐约看见魔族军队的大本营。整合骑士们打算在魔族入侵前发动一次奇袭，袭击魔族大本营！

为了降低风险，爱丽丝找到了你，一名优秀斥候，希望你能在奇袭前对魔族大本营进行侦查，并计算出袭击的难度。

经过侦查，你绘制出了魔族大本营的地图，然后发现，魔族大本营是一个N×N的网格图，一共有N支军队驻扎在一些网格中（不会有两只军队驻扎在一起）。

在大本营中，每有一个k×k(1≤k≤N)的子网格图包含恰好k支军队，我们袭击的难度就会增加1点。

现在请你根据绘制出的地图，告诉爱丽丝这次的袭击行动难度有多大。

## 【输入格式】

第一行，一个正整数N，表示网格图的大小以及军队数量。

接下来N行，每行两个整数,Xi,Yi，表示第i支军队的坐标。

**保证每一行和每一列都恰有一只军队,即每一个Xi和每一个Yi都是不一样的。**

## 【输出格式】

一行，一个整数表示袭击的难度。

##【输入输出样例】

```
5
1 1
3 2
2 4
5 5
4 3
```

```
10
```

## 【样例解释】

显然，分别以(2,2)和(4,4)为左上,右下顶点的一个子网格图中有3支军队，这为我们的难度贡献了1点。

类似的子网格图在原图中能找出10个。

## 【数据范围】

对于30%的数据，N ≤ 100

对于60%的数据，N ≤ 5000

对于100%的数据，N ≤ 50000

# Result

| 排名 | 名称       | gift | message | raid | 总分 |
| ---- | ---------- | ---- | ------- | ---- | ---- |
| 1    | std        | 100  | 100     | 100  | 300  |
| 1    | 陈浩然     | 100  | 100     | 100  | 300  |
| 3    | zhaojinxi  | 100  | 100     | 70   | 270  |
| 4    | EB-XYH     | 100  | 100     | 30   | 230  |
| 4    | rsq        | 100  | 30      | 100  | 230  |
| 6    | lhm        | 100  | 20      | 100  | 220  |
| 7    | yxt        | 0    | 100     | 100  | 200  |
| 8    | EN-WKP     | 10   | 100     | 70   | 180  |
| 9    | EN-YWK     | 40   | 100     | 30   | 170  |
| 9    | wyx        | 100  | 10      | 60   | 170  |
| 11   | EN-HHD     | 100  | 0       | 60   | 160  |
| 12   | YCS        | 50   | 100     | 0    | 150  |
| 13   | EN-ZLY     | 10   | 100     | 30   | 140  |
| 14   | EN-LYM     | 50   | 10      | 60   | 120  |
| 15   | EB-LY      | 100  | 0       | 0    | 100  |
| 15   | EB-WZX     | 100  | 0       | 0    | 100  |
| 15   | EN-YFT     | 100  | 0       | 0    | 100  |
| 15   | EN-shikeyu | 100  | 0       | 0    | 100  |
| 15   | EZ-axy     | 0    | 0       | 100  | 100  |
| 20   | lcy        | 50   | 0       | 30   | 80   |
| 21   | EN-Liang   | 50   | 0       | 0    | 50   |
| 21   | EN-WZB     | 50   | 0       | 0    | 50   |
| 21   | EN_Lijz    | 10   | 10      | 30   | 50   |
| 24   | EN-LZX     | 0    | 10      | 30   | 40   |
| 24   | EN-PJ      | 10   | 0       | 30   | 40   |
| 24   | EN-YZQ     | 10   | 0       | 30   | 40   |
| 24   | tyf        | 10   | 0       | 30   | 40   |
| 28   | lkx        | 0    | 0       | 30   | 30   |
| 29   | EN -MXT    | 10   | 10      | 0    | 20   |
| 29   | SM-LJH     | 10   | 10      | 0    | 20   |
| 31   | EB-Lxk     | 0    | 10      | 0    | 10   |
| 31   | Ez-LKX     | 0    | 10      | 0    | 10   |
| 33   | EN-TianNan | 0    | 0       | 0    | 0    |
| 33   | EN-ZC      | 0    | 0       | 0    | 0    |
| 33   | EN-zys     | 0    | 0       | 0    | 0    |