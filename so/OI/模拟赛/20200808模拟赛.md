---
title: 20200808模拟赛
tags:
  - OI
  - 模拟赛
categories:
  - [OI, 模拟赛]
mathjax: true
date: 2020-08-08 08:40:00
---

临行前的最后一场比赛

<!--more-->

# time

[P6005 [USACO20JAN]Time is Mooney G](https://www.luogu.com.cn/problem/P6005)

## 题目描述

Bessie 正在安排前往牛尼亚的一次出差，那里有 $N$（$2 \leq N \leq 1000$）个编号为 $1 \ldots N$ 的城市，由 $M$（$1 \leq M \leq 2000$）条单向的道路连接。Bessie 每次访问城市 $i$ 都可以赚到 $m_i$ 哞尼（$0 \leq m_i \leq 1000$）。从城市 $1$ 出发，Bessie 想要赚到尽可能多的哞尼，最后回到城市 $1$。为了避免争议，$m_1=0$。

沿着两个城市之间的道路移动需要消耗一天。出差的准备工作十分费钱；旅行 $T$ 天需要花费 $C \times T^2$ 哞尼（$1 \leq C \leq 1000$）。

Bessie 在一次出差中最多可以赚到多少哞尼？注意有可能最优方案是 Bessie 不访问城市 $1$ 之外的任何城市，在这种情况下结果应当为 $0$。

## 输入输出格式

### 输入格式

输入的第一行包含三个整数 $N$、$M$ 和 $C$。

第二行包含 $N$ 个整数 $m_1,m_2,\ldots, m_N$。

以下 $M$ 行每行包含两个空格分隔的整数 $a$ 和 $b$（$a \neq b$），表示从城市 $a$ 到城市 $b$ 的一条单向道路。

### 输出格式

输出一行，包含所求的答案。

## 输入输出样例

### 输入样例 #1

```
3 3 1
0 10 20
1 2
2 3
3 1
```

### 输出样例 #1

```
24
```

## 说明

最优的旅行方案是 $1 \to 2 \to 3 \to 1 \to 2 \to 3 \to1$。Bessie 总共赚到了 $10+20+10+20-1 \times 6^2=24$ 哞尼。

## 题解

详见<a href="../fen-ceng-tu-zui-duan-lu-xue-xi-bi-ji/#USACO20JAN-Time-is-Mooney-G">分层图最短路</a>。

# threesum

## 题解

最暴力的方法：直接枚举

期望得分：0？

考虑稍微优化一点。

这肯定是离线询问，也许还套着分治。但是我不会

把这个区间搞出来，sort一遍，枚举最左边的数，当这个大于零的时候停止。然后在它的右边维护两个指针，寻找两个数加起来等于左边数的相反数的个数。肯定有随着左指针的右移，右指针单调左移，所以这个应该是$O(r-l +1)$的，套上外面的枚举$O(len^2)$。已经优化不少了。但这还是一个在线算法。

# boards

## 题解

先考虑部分分做法（$P\le1000$）

观察这个$P$的大小，发现是可以$O(P^2)$过的，或者再加个$\log$。因为网格图中的其他数都没有用，所以考虑把这$P$个跳板当做节点，变成一个网格图，结束节点向$(N,N)$和右上方的开始节点连边权为曼哈顿距离的边，开始节点与$(0,0)$连曼哈顿距离的边，开始节点与结束节点连边权为$0$的边，边权非负，堆优化的Dijkstra可以过。

满分咋做呢？不如先换个思路

我们发现其实建了很多很多冗余的边，有的根本用不上。因为坐标是单调增加的，所以用跳板比不用一定会更优。

我们依旧考虑只留下这些跳板的坐标，来一个DP。因为这张图上没有后效性。

设$f[i][j]$表示走到$(i,j)$的最小步数，开始状态就是$f[0][0]=0$，结束状态是$f[N][N]$。（实际上是离散化后的点坐标）

转移的话，想维护哪来的不太容易，但是维护想去哪很容易。

所以我们考虑这样一个转移
$$
f[i+1][j]=\min(f[i+1][j], f[i][j] + 1)\\
f[i][j+1]=\min(f[i][j+1], f[i][j] + 1)\\
\mbox{if }plant\mbox{ then }f[rx][ry]=\min(f[rx][ry],f[lx][ly])
$$
我们只需要维护一个什么东西，让我们能知道这个地方有一个跳板，可以跳到哪。

然而可能细心的朋友发现了，$P\le10^5$，这玩意一定会炸裂的。

我们要考虑的是一个复杂度近似于$O(P\sqrt P)$的玩意，毕竟$P$不是$10^6$，那样就会是$O(P\log P)$了。

什么玩意复杂度带根号呢？

分块。

。。。。不会

